hello world


## Commands


```
git init
git add <fileName>
git add -A
git commit -m "<msg>"
git checkout - b <name>
git checkout <branchName>
git status
git merge <branchName>

git tag -a "<semVersion>" -m "<msg>"

git remote add <origin> <url>
git pull origin <branchName>
git push origin <branchName>
git push origin --tags

git reset --hard <id>
git log
git reflog
```